/** CMS fetch preloader — same positioning as work grid (top-biased, avoids jump when content mounts). */
export default function CmsSpinnerSlot() {
  return (
    <div className="portfolio-spinner-slot">
      <div className="spinner" />
    </div>
  );
}
